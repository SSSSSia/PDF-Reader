# PDF 双语阅读器 —— 环境变量示例
#
# 应用主配置位于 config/config.json（开发态）/ %APPDATA%/pdf-reader/config.json（Windows）
# 或 ~/.pdf-reader/config.json（其他平台）。API Key 等敏感信息写在 config.json 中。
#
# 本文件为可选：仅用于开发态覆盖配置文件路径，不需要可不创建。

# 用自定义配置文件路径覆盖默认位置（测试 / 自定义部署时有用）
# PDF_READER_CONFIG=/absolute/path/to/config.json
