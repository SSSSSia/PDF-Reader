# 交接提示词（2026-09-11 → 新会话续开发）

> 用法：新电脑/新会话 clone 或进入本仓库后，把下面整段作为首个提示词发给 AI 助手。

---

我在开发一款 PDF 双语对照阅读器（Tauri 2 + React/TS 前端 + Python FastAPI sidecar 后端，
学术工具，开源复现，分支 `phase-1`）。请先读 `docs/README.md`（文档唯一入口）→
`docs/重构路线图.md`（状态跟踪表）→ 再按需读各阶段文档，然后等我指派任务。

## 项目当前状态（截至 2026-09-11 晚）

- **验收里程碑（2026-09-11）**：阶段 1/2/3/5/6/8/10 人工验收通过，状态表已转 ✅；
  阶段 7 随阶段 10 收尾（T5 双栏缩放复验被阶段 10 覆盖）。**用户确认有少量遗留
  问题但明细未给——需向用户要明细并回填路线图 §4.1 占位**
- **阶段9 BabelDOC（v0.11.0）**：T0–T3' 完成。今天验收期修复（5dad312）：进入
  「原版PDF·左右对照」不再自动启动 BabelDOC——主翻译中门禁卡、空闲时确认卡
  （起因：翻译 99% 时启动 worker 与 pdfjs 并发把 16GB 内存榨尽，Windows RADAR
  资源耗尽预警实证，WebView2 崩溃重载）。待做：**T4**（权重下载失败引导/429
  退避/大文档分批）、**T5**（AGPL 声明，10 分钟小任务）、**T6**（今天立项：
  BabelDOC 可选组件化打包，用户点击同意后应用自动安装，分发载体待设计）
- **阶段11（v0.13.0）**：立项未动工。T0 性能基线是决策门；T1（memo+局部更新）
  首因已实证可先做；T2（轮询合并+并发队列）因今天的内存事故优先级上调
- **其他阶段**：阶段 7 标记完成；无其他开发线在途

## ⚠️ 发布清单进行到一半（新会话最优先接力项，用户挂好代理后执行）

用户已确认 **tag 方案 A：只打 v0.12.0 一个 tag**（不按阶段补打七个）。清单进度：

- ✅ tsc/vite build、cargo check（Rust 1.96 本机可用）、sidecar exe 已打包、
  总纲/路线图/README 状态已同步并提交
- ⬜ **卡在**：`npm run tauri build` 的 WiX 工具包下载（GitHub 直连极慢/挂死）。
  半截包在 `%TEMP%\wix314-binaries.zip`（18MB/50MB，curl `-C -` 可断点续传）
- 接力步骤：挂代理 → `curl -L -C - --retry 3 -o "$TEMP/wix314-binaries.zip"
  https://github.com/wixtoolset/wix3/releases/download/wix3141rtm/wix314-binaries.zip`
  续传完（约 50MB）→ 解压覆盖 `%LOCALAPPDATA%\tauri\WixTools314\`（删旧目录）
  → 重跑 `npm run tauri build` → 产物在 `src-tauri/target/release/bundle/`
  → 通过后打 `v0.12.0` 附注 tag → 本地全可快进合并（origin/master→dev→phase-1
  线性无分叉）：`git checkout dev && git merge phase-1 && git checkout -b master
  origin/master && git merge dev` → push（本地领先远端 8+ 提交，含 tag）

## 环境要点（本机环境 2026-09-11 已重建完毕，无需再建）

- 后端：项目根 `.venv\Scripts\python.exe backend/main.py`（127.0.0.1:8000）；
  前端 `npm run dev`（5173 浏览器模式）或 `npm run tauri dev`（桌面完整体验）；
  一键 `powershell -ExecutionPolicy Bypass -File scripts\dev-start.ps1`
  （后端日志落 `logs/backend-dev.log`）
- `.venv` 与 `.venv-babeldoc` 均为 CPython 3.12.11（uv 管理），BabelDOC==0.6.4
  已装并验证；sidecar `pdf-backend-x86_64-pc-windows-msvc.exe` 已打包在
  `src-tauri/sidecar/`；机器 16GB 内存
- 用户数据：`%APPDATA%/pdf-reader/`（config.json 已配好 SiliconFlow key；
  cache/ 含翻译缓存与 babeldoc 产物——上次排版对照生成未完成，重进模式需重新生成）
- 免费档模型：Qwen/Qwen3-8B 翻译 + PaddleOCR-VL-1.5（OCR max_tokens ≤8192）

## 工作约定（强约束）

- **有一定修改就及时提交 git**（2026-09-11 新增地基约定，见开发流程管理 §2 /
  README §4）；UI 改动先出 mockup 经用户确认再落地；蓝色仅交互/翻译语义；
  原生对话框一律自绘
- 阶段完成必须回填阶段文档 + 更新路线图状态表；接口/数据模型改动同步总纲 §4.4
- 用户以截图反馈问题，先根因分析再修复（今天崩溃即凭 Windows 事件日志
  RADAR 事件定位）；人工测试优先，不做自动部署
- 模型配置禁止硬编码；OCR max_tokens ≤8192；API key 不进日志/记忆/对话

## 建议执行顺序

代理收尾发版（上节）→ 阶段9-T5（AGPL 声明，顺手小任务）→ 阶段11-T1（立竿见影）
→ 阶段9-T4 → 阶段11 T0-T3 → 阶段9-T6（发布前定分发载体设计，工作量最大）。
