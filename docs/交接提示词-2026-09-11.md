# 交接提示词（2026-09-11 晚 → 新会话续开发）

> 用法：新电脑/新会话 clone 或进入本仓库后，把下面整段作为首个提示词发给 AI 助手
> （或直接说「读 docs/交接提示词-2026-09-11.md 并按其执行」）。

---

我在开发一款 PDF 双语对照阅读器（Tauri 2 + React/TS 前端 + Python FastAPI sidecar 后端，
学术工具，开源复现，分支 `phase-1`）。请先读 `docs/README.md`（文档唯一入口）→
`docs/重构路线图.md`（状态跟踪表）→ 再按需读各阶段文档，然后等我指派任务。

## 项目当前状态（截至 2026-09-11 深夜）

- **验收里程碑（2026-09-11）**：阶段 1/2/3/5/6/8/10 人工验收通过（状态表 ✅，
  阶段 7 并入阶段 10 收尾）；**无遗留问题**（历史遗留"上传页崩溃"不再成立）。
  同日发布清单首次完整跑通，打 tag **`v0.12.0`**（方案A：历史里程碑并入、不补打），
  dev/master/phase-1 三线齐平已推送。
- **阶段9 BabelDOC（v0.11.0）**：T0–T5 全部完成（T4 失败边界：权重失败引导/
  429 qps 减半重跑≤3 次/大文档 `--max-pages-per-part 200` 分批；T5 AGPL 合规：
  README 第三方组件节 + 设置页声明）。验收期修复：进入「原版PDF·左右对照」
  不自动启动——主翻译中门禁卡、空闲时确认卡 + **缓存探测**（`GET
  /api/export/babeldoc/cached`，已生成文档免确认直接打开）。**仅剩 T6：可选
  组件化打包**（主包不带 BabelDOC，一键同意安装；分发载体/安装位/探测链待设计）。
- **阶段11 性能与并发（v0.13.0）**：T0–T4 自动化全部完成——T0 基线
  （`backend/tools/perf_baseline.py`，四场景 health P95 7.2–10.0ms，达标线
  200ms，判定通过）；T1 渲染优化落地（MarkdownText/行组件 memo +
  applyBlockPatches 局部更新替代整表 setPages）；T2/T3 按 T0 裁剪（并发守卫
  翻译≤2/worker≤2 + worker RSS>2GB 告警；进程池/轮询合并/多进程裁掉）。
  **待人工复验**：流式翻译体感 + 人工跨模式清单。另立项 **T5 翻译任务自动
  重接管**（F5 后一键恢复，拆解见阶段11 文档）。
- **待拍板候选改进**（已记路线图 §4.1，等用户点头）：① `/reader/*` 无会话时
  路由守卫自动回主页（替代"请先上传 PDF"死页面，F5 后体验更好）；② 打包版
  拦截 F5/Ctrl+R 防误触丢会话（dev 不受影响）。

## 环境要点（本机环境 2026-09-11 已重建完毕，无需再建）

- 后端：项目根 `.venv\Scripts\python.exe backend/main.py`（127.0.0.1:8000）；
  前端 `npm run dev`（5173 浏览器模式）或 `npm run tauri dev`（桌面完整体验）；
  一键 `powershell -ExecutionPolicy Bypass -File scripts\dev-start.ps1`
  （后端日志落 `logs/backend-dev.log`）
- `.venv` 与 `.venv-babeldoc` 均为 CPython 3.12.11（uv 管理），BabelDOC==0.6.4
  已装；sidecar 已打包；打包中间件 WiX 缓存在 `%LOCALAPPDATA%\tauri\WixTools314`
  （重打包无需再下载）；双安装包产物在 `src-tauri/target/release/bundle/`
- 用户数据：`%APPDATA%/pdf-reader/`（config.json 已配 SiliconFlow key；翻译缓存
  与 babeldoc 产物在 cache/）；测试文档 DALK.pdf（19 页，翻译缓存近全命中）
- 免费档模型：Qwen/Qwen3-8B 翻译 + PaddleOCR-VL-1.5（OCR max_tokens ≤8192）

## 工作约定（强约束）

- **有一定修改就及时提交 git**（地基约定，开发流程管理 §2 / README §4）；
  **运行中的服务不可擅动**——重启后端/查杀端口/跑压测前必须确认用户没在用
  （README §4.7；2026-09-11 压测误杀用户翻译的事故防复发）
- UI 改动先出 mockup 经用户确认再落地；蓝色仅交互/翻译语义；原生对话框一律自绘
- 阶段完成必须回填阶段文档 + 更新路线图状态表；接口/数据模型改动同步总纲 §4.4
- 用户以截图反馈问题，先根因分析再修复；人工测试优先，不做自动部署
- 模型配置禁止硬编码；OCR max_tokens ≤8192；API key 不进日志/记忆/对话

## 建议执行顺序

用户人工验收（阶段9 排版对照质量确认 + 阶段11 体感/跨模式）→ 阶段11-T5（翻译
自动重接管，小任务）→ 阶段9-T6（发布前集中做，先与用户定分发载体设计）→ 候选
改进两项（等拍板）→ v0.13.0 / v0.14.0 收官发版。开发线剩 T5/T6 两项。
