# 交接提示词（2026-09-12 → 新会话续开发）

> 用法：新电脑/新会话 clone 或进入本仓库后，把下面整段作为首个提示词发给 AI 助手
> （或直接说「读 docs/交接提示词-2026-09-12.md 并按其执行」）。

---

我在开发一款 PDF 双语对照阅读器（Tauri 2 + React/TS 前端 + Python FastAPI sidecar 后端，
学术工具，开源复现，分支 `phase-1`）。请先读 `docs/README.md`（文档唯一入口）→
`docs/重构路线图.md`（状态跟踪表）→ 再按需读各阶段文档，然后等我指派任务。

## 项目当前状态（截至 2026-09-12 晚）

- **2026-09-11 里程碑不变**：阶段 1/2/3/5/6/8/10 人工验收通过；tag `v0.12.0`
  已发布（dev/master/phase-1 三线齐平）。
- **阶段11 性能与并发（v0.13.0）：开发全部完成**——
  - T5 翻译任务自动重接管 ✅：F5 后启动发现弹窗（继续翻译/忽略）、attach 复用
    轮询、上传即入库（docs_index 早期 upsert）、打开文献接管提示、BabelDOC
    静默重接管 + F5 视图洞修复（阅读页门禁对重接管任务放行）；
  - T6 进入时机门禁 ✅：`EXTRACT_DONE` 30→31 移 translationManager 共用，
    进度卡/标题栏 tab/「继续翻译」弹窗三个入口统一门禁；
  - 待**人工复验**：跨模式清单 + 体感（19 页文档已验"无可感知卡顿"）→ 通过后
    打 **v0.13.0**。
- **2026-09-12 用户反馈四项全闭环**（路线图 §4.1）：① 删除文件夹蒙版失效
  （ConfirmDialog portal 到 body，sticky 容器 stacking context 根因）；② 启动
  白屏 splash（窗口 visible:false + 静态品牌页 + 挂载后 show()，**需完整重启
  tauri dev/重打包生效**）；③ 标题识别系统性缺失根治（降级规则收窄 + 页 0 首
  标题豁免 + 拆分规则标题豁免 + `TEXT_LAYER_MODEL` v16→v17 熔断；缓存失效
  死路同步修复——点卡片弹确认一键重提取）；④ 进入时机 = 阶段11-T6。
- **2026-09-12 联调修复三项**：⑤ F5 后对照视图不可达；⑥ 进度双源歧义（底栏
  标明文档名）+ 标题栏冻结 tab 按中断呈现；⑦ 对照视图跨文档状态污染
  （babeldocStore 按路径归属采信 + 他篇生成中禁启本篇内存守卫）。
- **阶段9-T6 定稿为「随包捆绑」**（两次迭代：先实现可选下载 661a85b→49d6a5e，
  用户复核后反转，0cecd87 定稿）——目标用户非计算机专业、体积不敏感，随包
  分发最可控：`build-babeldoc-runtime.ps1` 暂存运行时（embeddable python +
  venv site-packages，冒烟自检）→ `build-exe.ps1` 串联 → tauri resources 落位
  后端 exe 同级；检测链 venv → exe 同级 → 数据目录（保留位）。体积代价用户已
  接受（安装包 ~72MB→约 400MB）。**下载链路已整体移除**（git 历史可考）。
- **验证基线**：pytest 149/6、tsc 0 错误、vite build 通过（2026-09-12 全绿）。
- **文档已同步**：路线图状态表/§4.1、阶段9 §6+T6、阶段11 T5/T6、阶段
  2/7/8/10 陈旧勾选回填、提取质量体检 §标题根治、README/总纲接口表。

## 剩余开发计划（全部）

1. **人工验收批（等用户操作，非开发任务）**：
   - 标题根治：重译 DALK/FG-RAG，看重**排版/紧跟**视图首页标题（对照视图是
     BabelDOC 自己的管线，不能作验收依据）；
   - ①②⑤⑥⑦修复效果确认（splash 需重启 tauri dev/重打包才生效）；
   - 阶段11 人工跨模式复验清单。
2. **候选改进两项（等用户拍板）**：① `/reader/*` 无会话路由守卫回主页
   （BabelDOC 场景已被门禁放行覆盖，仅剩纯无任务场景）；② 打包版拦截
   F5/Ctrl+R 防误触丢会话。
3. **发布期动作（v0.13.0 收官）**：有网实跑 `build-exe.ps1`（在线下载
   embeddable python ~11MB）→ 干净环境人工验收（临时改名 `.venv-babeldoc`，
   确认打包版对照开箱即用）→ 打 tag v0.13.0 → 三线合并推送。
4. **可选项（无排期）**：阶段2-T5 SSE 流式推送（轮询已满足需求）。

## 环境要点（本机环境已建好，无需重建）

- 后端：项目根 `.venv\Scripts\python.exe backend/main.py`（127.0.0.1:8000）；
  前端 `npm run dev`（5173 浏览器模式）或 `npm run tauri dev`（桌面完整体验）；
  一键 `powershell -ExecutionPolicy Bypass -File scripts\dev-start.ps1`
  （后端日志落 `logs/backend-dev.log`）
- `.venv` 与 `.venv-babeldoc` 均为 CPython 3.12.11（uv 管理），BabelDOC==0.6.4
  已装；sidecar 已打包；WiX 缓存在 `%LOCALAPPDATA%\tauri\WixTools314`；
  `dist/` 为构建暂存（gitignore，babeldoc-runtime 捆绑源）
- 用户数据：`%APPDATA%/pdf-reader/`（config.json 已配 SiliconFlow key；翻译缓存
  与 babeldoc 产物在 cache/）；测试文档 DALK.pdf（19 页）/ FG-RAG.pdf（5 页），
  源文件在 `E:\ZiLiao\论文阅读\GraphRAG&KGQA\`
- 免费档模型：Qwen/Qwen3-8B 翻译 + PaddleOCR-VL-1.5（OCR max_tokens ≤8192）

## 工作约定（强约束）

- **有一定修改就及时提交 git**（地基约定）；**运行中的服务不可擅动**——重启
  后端/查杀端口/跑压测前必须确认用户没在用（README §4.7）
- UI 改动先出 mockup 经用户确认再落地；蓝色仅交互/翻译语义；原生对话框一律自绘
- 阶段完成必须回填阶段文档 + 更新路线图状态表；接口/数据模型改动同步总纲 §4.4
- 用户以截图反馈问题，先根因分析再修复；人工测试优先，不做自动部署
- 模型配置禁止硬编码；OCR max_tokens ≤8192；API key 不进日志/记忆/对话

## 建议执行顺序

人工验收批 → 候选改进拍板（做则先小任务）→ 发布期动作（build-exe 实跑 +
干净环境验收）→ v0.13.0 打包发版收口阶段9/11。**开发任务已清零**，剩余全部
是验收、拍板与发布动作。
