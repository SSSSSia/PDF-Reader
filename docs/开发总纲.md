# PDF 双语对照阅读器 — 开发总纲（单一事实来源 / Single Source of Truth）

> **地位**：本文件是项目最高优先级的约束文件。所有后续开发（含 agent 自动执行）必须以本文件为准，不得偏离。任何技术决策若与本文件冲突，必须先更新本文件并经确认。
> **版本**：v2.0 ｜ **制定日期**：2026-09-04 ｜ **制定依据**：对现有仓库代码逐文件审计（非空想）
> **一句话**：本地优先的 PDF 双语对照阅读/翻译桌面软件（上传 PDF → OCR → LLM 翻译 → 左右对照 / 译文紧跟）。
> **当前状态**：**Phase 0 已完成** — P0 缺陷 R1/R2/R3/R4 已修复，主链路打通（详见第 5、6 章）。P1/P2 待处理；**打包需在具备 Windows 10/11 SDK 的环境验证**（见第 10 章）。

---

## 0. 文档关系说明

- 本文件**取代**原 `pdf-bilingual-reader-dev-guide.md`。旧版与代码严重不符：配置字段误写为 camelCase（实为 snake_case）、承诺了未实现的能力（OpenAI/Google/DeepL provider、Docker、LICENSE 等）、架构描述理想化（假设流水线同步返回结果）。旧文件已标记作废，请勿再据其开发。
- 后续若需细分文档（接口契约、打包手册、复刻指南），作为本文件的子章节或独立附录，**不得与本文冲突**。

---

## 1. 产品愿景与范围（锁定）

### 1.1 目标
- **个人本地使用**：配置一次 API Key，开箱即可双语阅读。
- **开源复刻**：他人 clone 后按文档可跑起来（平台范围见第 9 章 Q2）。

### 1.2 核心功能（MVP 必含）
| 功能 | 描述 |
|------|------|
| PDF 上传 | 选择/拖拽 PDF |
| OCR 识别 | SiliconFlow PaddleOCR-VL-1.5 提取文本+版面 |
| 段落级翻译 | 调用 LLM 按 block 翻译 |
| 左右对照 | 双栏同步滚动（原文/译文） |
| 紧跟模式 | 原文段下嵌入译文 |
| 配置页 | 填写 API Key，可切换 provider |
| 本地缓存 | 已翻译结果落盘，避免重复调用 |

### 1.3 明确不做（非目标）
- 不做 Word/Excel/PPT 等其它格式（仅 PDF）。
- 不本地部署 OCR/翻译模型（全部走云端 API，exe 不含模型权重）。
- 不做在线账号 / 多用户 / 协作。
- 不做移动端。

---

## 2. 技术选型（锁定，禁止替换）

以下栈基于**现有代码已落地**的真实情况，不是建议：

| 层 | 技术 | 现状 |
|----|------|------|
| 桌面壳 | Tauri v2（Rust 2021 edition） | ✅ 已搭 |
| 前端 | React 18 + TypeScript 5 + Vite 5 | ✅ 已搭 |
| 样式 | Tailwind CSS 3 | ✅ 已搭 |
| 状态 | Zustand 4 | ✅ 已搭 |
| 后端 | FastAPI（Python 3.10+） | ✅ 已搭（开发沙盒态） |
| OCR | SiliconFlow PaddleOCR-VL-1.5 | ✅ 已接（代码就绪；**真实 API 待填入 Key 联调**） |
| 翻译 | SiliconFlow（OpenAI 兼容协议） | ✅ SiliconFlow/OpenAI 可用（共用 OpenAI 兼容实现）；Google/DeepL 待实现（UI 已禁用） |
| 打包 | `tauri build` + sidecar | ✅ 后端以 sidecar 内嵌（D1）；需 Windows 10/11 SDK 环境构建验证 |

### 2.1 硬性约束
1. **禁止**引入 Electron。
2. **禁止**在前端直连第三方 API 或硬编码 Key；Key 只经 Rust 落盘到 `config.json`。
3. OCR 默认且唯一走 SiliconFlow PaddleOCR-VL-1.5（免费）；不做本地 OCR 模型。
4. 翻译默认 SiliconFlow；采用**可扩展 provider 抽象**（注册表 + 统一接口，见 4.3）。默认实现 SiliconFlow；UI 列表提供主流提供商（SiliconFlow、OpenAI 及兼容端、Google、DeepL 等）。OpenAI 兼容协议（OpenAI/DeepSeek/各家兼容端）共用一套 chat/completions 实现，仅 `api_url`/`api_key`/`model` 不同；Google/DeepL 为独立适配器，未实现前在 UI 标注“即将支持”，**不允许“UI 有选项、后端无实现”**。
5. 通信链：前端 `invoke` → Rust 命令 → HTTP → FastAPI → 第三方 API。

---

## 3. 架构与数据流（基于真实代码）

### 3.1 分层
```
React 前端 (src/)
   │  invoke (Tauri 命令)
   ▼
Rust 后端 (src-tauri/src/commands.rs)  —— 进程内，reqwest 调本地 8000
   │  HTTP POST/GET
   ▼
FastAPI 后端 (backend/)  —— 127.0.0.1:8000
   │  HTTPS + Bearer Key
   ▼
SiliconFlow API（OCR /generate、翻译 /chat/completions）
```

**分发方式（已确认 D1）**：FastAPI 以 **Tauri sidecar** 形式随 exe 打包（内嵌 Python 解释器 + venv），应用启动由 Rust 拉起本地 8000 端口；分发包仅 Windows（D2）。

### 3.2 当前真实数据流（含断点）
1. 用户在 `MainPage` 选 PDF（dialog）→ **立即**调用 `run_pipeline`（注：当前无“开始识别”按钮，选完即处理，与旧文档不符）。
2. Rust `run_pipeline` → `POST /api/pipeline/run`。
3. FastAPI `run_pipeline` 立即返回 `{job_id, status:"running"}`，并 `asyncio.create_task` 后台处理（**fire-and-forget，未持有任务句柄**）。
4. 后台：`call_ocr`（PDF 转 base64 → SiliconFlow `/generate`）→ 逐 block 调 `translate_text`。
5. 结果仅存内存 `_jobs[job_id]`。
6. **断点①（致命）**：前端 `useOcr` 只读取首次返回的 `running`，**从不轮询 `/api/pipeline/status/{job_id}`** → 结果页面永远不刷新 → 应用当前“不可用”的根因。
7. **断点②**：`BilingualPage` / `InlinePage` 只渲染 `pages[0]`，无多页导航、`PdfViewer` 是空桩。

### 3.3 目标链路
前端轮询 status（或 SSE/WebSocket）直到 `done`/`failed` → 拿到 `pages` → 渲染（含多页导航）。

---

## 4. 数据模型与接口契约（锁定）

### 4.1 配置文件（磁盘）
路径：`~/.pdf-reader/config.json`（Windows：`%APPDATA%/pdf-reader/config.json`）。
**字段全部 snake_case**（旧文档 5.1 误写 camelCase `apiKey`/`apiUrl`，以本文件为准）：
```json
{
  "ocr": {
    "provider": "siliconflow",
    "api_key": "",
    "api_url": "https://api.siliconflow.cn/v1",
    "model": "PaddlePaddle/PaddleOCR-VL-1.5",
    "optional_payload": { "useDocOrientationClassify": false, "useDocUnwarping": false, "useChartRecognition": false }
  },
  "translate": {
    "provider": "siliconflow",
    "api_key": "",
    "api_url": "https://api.siliconflow.cn/v1",
    "model": "Qwen2.5-7B-Instruct",
    "target_language": "en",
    "source_language": "zh"
  },
  "ui": { "default_mode": "bilingual", "theme": "light" }
}
```

### 4.2 核心数据结构（前后端一致）
- `TextBlock { block_id:int, page:int, original:str, translated:str, position:{y_start:int,y_end:int} }`
- `PageResult { page:int, blocks:TextBlock[] }`
- `PipelineResult { job_id:str, status:"running"|"done"|"failed", progress:int, pages:PageResult[] }`
（TS 定义见 `src/types/index.ts`，Rust 定义见 `src-tauri/src/commands.rs` 的 `AppConfig` 等。）

### 4.3 翻译 Provider 接口契约（可扩展抽象，已确认 D3）
采用**注册表 + 统一接口**模式，便于复刻者自行接入新厂商：
- 统一接口（所有 provider 必须实现）：
  - `translate(text, source_lang, target_lang, config) -> str`
  - `translate_batch(texts, source_lang, target_lang, config) -> list[str]`
- 注册表：`PROVIDERS = { "siliconflow": ..., "openai": ..., ... }`，由 `provider` 字段分发。
- **默认实现**：`siliconflow`（OpenAI 兼容 chat/completions）。
- **OpenAI 兼容族**：OpenAI 及一切 OpenAI 兼容端点（DeepSeek、Qwen、各家兼容服务）共用一个 `openai_compatible` 实现，仅 `api_url`/`api_key`/`model` 不同 → 一次实现覆盖多数厂商。
- **独立适配器**：Google Translate、DeepL 协议不同，作为独立 provider 后续实现；未实现前 UI 列表保留但标注“即将支持”，禁止静默调用失败。
- 现状：后端仅 `siliconflow` 实现（`translate/base.py` 函数式）；`openai`/`google`/`deepl` 后端未实现（见 6.R8）。

### 4.4 FastAPI 接口（现状真实）
| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/api/health` | 健康检查：Tauri 侧据此判断内嵌 sidecar 已就绪 |
| POST | `/api/pipeline/run` | `{file_path} → {job_id, status:"running"}`（**异步**，须轮询 status） |
| GET | `/api/pipeline/status/{job_id}` | `{job_id, status, progress, pages, error}` |
| POST | `/api/ocr/process` | `{file_path} → pages` |
| POST | `/api/translate/batch` | `{texts, source_lang, target_lang} → [str]` |
- 监听 `127.0.0.1:8000`，CORS `*`，无 Pydantic 校验（直接用 dict）。

### 4.5 Tauri 命令（现状真实）
`load_config` / `save_config` / `run_pipeline` / `get_pipeline_status` / `check_file_exists` / `get_cache_dir`

---

## 5. 当前实现状态（逐文件审计）

图例：✅ 完成 ｜ ⚠️ 部分（有坑/桩） ｜ ❌ 缺失

### 后端（backend/）
| 文件 | 状态 | 说明 |
|------|------|------|
| `main.py` | ✅ | 路由与 CORS 正常；已加 `/api/health` 与 `/api/config/reload` |
| `config.py` | ✅ | `Settings` 按 mtime 自动热重载（R7 已修），处理任务前 `refresh()` |
| `ocr/siliconflow.py` | ⚠️ | 端点 `/generate`、返回 `result[].layoutParsingResults[].markdown.text` **未对真实 API 验证**（R6）；>50 页未分批 |
| `translate/base.py` | ⚠️ | 仅 siliconflow；空 Key 返回 `""`；无超时重试/限流 |
| `pipeline/processor.py` | ⚠️ | `get_pipeline_status` 已返回 `error`；仍待修：任务 fire-and-forget 未跟踪、**未接入缓存**（R5）、逐段串行无并发/限流（R9） |
| `cache/file_cache.py` | ⚠️ | 已实现但**未被任何地方调用** |
| `models/`（Pydantic） | ❌ | 缺失，请求体无类型校验 |

### 前端（src/）
| 文件 | 状态 | 说明 |
|------|------|------|
| `App.tsx` | ✅ | 路由与 `isConfigured` 守卫正常 |
| `stores/*` | ✅ | configStore / pdfStore / uiStore 基本可用 |
| `hooks/useOcr.ts` | ✅ | 已改为轮询 status 至 done/failed 并返回成功标志（**R1 已修**） |
| `hooks/useScrollSync.ts` | ✅ | 左右同步逻辑合理 |
| `components/MainPage.tsx` | ✅ | 选择即处理 + 进度条 + 错误提示 + 完成后跳转阅读页（拖拽待补） |
| `components/BilingualPage.tsx` | ✅ | 按 `currentPage` 分页渲染 + `PdfViewer` 导航（**R2 已修**）；block 高度按文字长度估算（待优化） |
| `components/InlinePage.tsx` | ✅ | 按 `currentPage` 分页渲染 + `PdfViewer` 导航（**R2 已修**） |
| `components/PdfViewer.tsx` | ✅ | 页码导航 + **pdfjs-dist 缩略图导航**（已确认引入，仅限缩略图）；`asset:default` 已补 |
| `components/ConfigPage.tsx` | ✅ | OCR 仅 SiliconFlow（移除无后端实现的飞桨星河）；翻译保留 SiliconFlow/OpenAI，Google/DeepL 标「即将支持」禁用（R8 已修） |
| `components/ExportBar.tsx` | ✅ | **新增**：导出双语 Markdown/纯文本（经 Rust `export_content` 写盘，浏览器回退 blob 下载） |
| `components/MainPage.tsx` | ✅ | 选择即处理 + 进度条 + 错误提示 + 完成后跳转；**新增拖拽上传**（Tauri drag-drop 事件） |
| `components/common/Layout.tsx` | ✅ | **新增暗色模式切换**与 `dark:` 变体（R13 已修） |
| `hooks/useTranslate.ts` | ❌ | 旧文档声称存在，实际缺失 |
| `utils/`（api/cache/file） | ❌ | 旧文档声称存在，实际缺失 |

### 桌面壳（src-tauri/）
| 文件 | 状态 | 说明 |
|------|------|------|
| `commands.rs` | ✅ | 7 个命令：`load_config`/`save_config`/`run_pipeline`/`get_pipeline_status`/`check_file_exists`/`get_cache_dir`/`export_content` |
| `main.rs` | ✅ | release 模式 spawn 内嵌 sidecar 并转发日志、退出时 kill 子进程（**R3 已修**）；dev 模式跳过，沿用外部后端 |
| `capabilities/default.json` | ✅ | 已补 `dialog:default` 与 sidecar 所需 `shell:allow-spawn/kill/stdin-write`（**R4 已修**） |
| `tauri.conf.json` | ✅ | 窗口 1400×900，csp=null；icons 齐全；已加 `bundle.externalBin: ["sidecar/pdf-backend"]` |
| `Cargo.toml` | ✅ | 依赖合理（tauri-plugin-shell/dialog、reqwest）；已移除误置于 `[dependencies]` 的 `tauri-build` |

### 脚本与根目录
| 项 | 状态 | 说明 |
|----|------|------|
| `scripts/dev-start.ps1` | ⚠️ | 分开起 `python` + `tauri dev`，非自包含 |
| `scripts/build-backend.ps1` | ✅ | **新增**：PyInstaller 将后端打包为 `src-tauri/sidecar/pdf-backend-<triple>.exe`（**R3**） |
| `scripts/build-exe.ps1` | ⚠️ | 仅 `cargo build` + `tauri build`，未串联后端打包步骤（应先跑 `build-backend.ps1`） |
| `check_api.py` / `find_tauri_build.py` | ❌ | 带绝对路径的调试脚本，应清理（R12） |
| `.gitignore` | ✅ | 存在 |
| `.env.example` / `LICENSE` / `Dockerfile` / 测试 | ❌ | 缺失（旧文档承诺过，实际无） |

---

## 6. 已知风险与关键缺陷（按严重度）

**P0（应用不可用）— ✅ 已全部修复（Phase 0）**
- ~~**R1** 前端不轮询流水线状态 → 结果不显示~~ → 已修：`useOcr.ts` 轮询至 done/failed。
- ~~**R2** 仅渲染 `pages[0]`，无多页/导航~~ → 已修：按 `currentPage` 分页 + `PdfViewer` 页码导航。
- ~~**R3** Python 后端未内嵌/自启动 → 打包后 exe 无法独立运行~~ → 已修：Tauri sidecar 内嵌（D1），见第 10 章。
- ~~**R4** capabilities 缺 `dialog` 权限 → 文件选择失败~~ → 已修：补 `dialog:default` 与 shell 权限。

**P1（正确性 / 体验）**
- ~~**R5** 翻译缓存 `file_cache` 未接入 pipeline~~ → 已修：两层缓存接入流水线，按页命中。
- ~~**R6** OCR 端点与真实 SiliconFlow API 未验证~~ → 已修复：重写为 `/chat/completions`（PDF 经 PyMuPDF 转图），原 `/generate` 实现调不通的问题消除。
- ~~**R7** 配置不热更新~~ → 已修：`config.py` 按 mtime 自动重载 + `POST /api/config/reload`。
- ~~**R8** 仅 SiliconFlow 翻译可用，UI 另三项为坏链接~~ → 已修：D3 provider 抽象；UI 移除无后端实现的选项，未实现项（Google/DeepL）标「即将支持」并禁用。
- **R9** 逐段串行 → 已加并发翻译（上限 4）；**限流/重试策略待补**（部分完成）。
- **R10** 明文保存 API Key（无加密）——侧边项，待定。

**P2（工程规范）**
- **R11** 无 Pydantic 校验、无测试、无 CI。
- **R12** 调试脚本残留（绝对路径）。
- **R13** 暗色模式死代码（`toggleTheme` 未接，`index.css` 有 `.dark` 但无切换）。
- **R14** 文档/代码字段命名不一致（旧 guide camelCase）。

---

## 7. 开发约束（强制，约束 agent 不跑偏）

1. 不得替换已锁定的技术栈（Tauri / React / Tailwind / Zustand / FastAPI / SiliconFlow OCR）。
2. 不得在前端直连第三方 API 或硬编码 Key；Key 只经 Rust 落盘 `config.json`。
3. 新增翻译 provider 必须实现 4.3 接口，并**同时**接入 `ConfigPage` 列表与后端分发逻辑；不允许“UI 有选项、后端无实现”。
4. 前端读取流水线结果**必须**基于轮询 `/api/pipeline/status/{job_id}`（或 SSE），**禁止**假设 `run` 同步返回 `done`。
5. 阅读页**必须**支持多页（分页或虚拟滚动），**禁止**只渲染 `pages[0]`。
6. 数据模型/接口字段以本文 4.x 为准（snake_case），改动须同步 TS 类型与 Rust 结构。
7. 缓存必须接入流水线（read → 翻译 → write），禁止“实现不用”。
8. 不得删除/重命名既有 Tauri 命令或 FastAPI 路由，除非先在本文 4.4/4.5 更新契约。
9. 修复缺陷须**优先消除 P0（R1–R4）**，再做 P1。
10. 禁止为“好看”引入新重型依赖（如 UI 组件库）。`pdfjs-dist` 已按 **D4** 经用户确认引入，**仅限用于缩略图导航**；其他重型依赖仍须经第 9 章决策确认方可引入。

---

## 8. 分阶段路线图（建议，可调整）

**Phase 0 — 打通主链路 ✅ 已完成**
- ✔ 修 R3/R4：按 **D1** 以 Tauri sidecar 内嵌 FastAPI（`externalBin` + 启动 spawn + 退出 kill），并补全 `capabilities`。
- ✔ 修 R1：`useOcr` 轮询 `/api/pipeline/status/{job_id}` 至 `done`/`failed`，回显 `pages` 并跳转阅读页。
- ✔ 修 R2：按 **D4** 实现分页导航（`PdfViewer` 页码导航 + `currentPage` 索引），并用 **pdfjs-dist**（已按 D4 确认引入）渲染真实页面缩略图导航，替换原空桩。
- ⬜ 收尾：在具备 Windows 10/11 SDK 的环境执行 `cargo check` 与 `tauri build` 验证（见第 10 章）。

**Phase 1 — 正确性与可靠性 ✅ 已完成**
- ✔ **R5 缓存接入**：重写 `cache/file_cache.py`（修复 `Optional` 未导入 bug），两层缓存接入流水线，按页命中。
- ✔ **R7 配置热更新**：`config.py` 按 mtime 自动重载 + `POST /api/config/reload`。
- ✔ **R6 OCR 重写**：`backend/ocr/siliconflow.py` 改为 SiliconFlow `/chat/completions`（PyMuPDF 转图 → 逐页 OCR），消除原 `/generate` 调不通的 P0 阻断。
- ✔ **R9（部分）**：asyncio 并发翻译（上限 4）+ 渐进回显 + 单 block 失败不中断；限流/重试策略待补。
- ⬜ R10 API Key 加密（侧边项，待定）。

**Phase 2 — Provider 与体验 ✅ 已完成**
- ✔ **D3 provider 抽象**：`translate/providers/` 抽象基类 + 注册表；SiliconFlow/OpenAI 共用 OpenAI 兼容实现；Google/DeepL 适配器桩（UI 标「即将支持」禁用），消除 R8 坏链接。
- ✔ **R13 暗色模式**：`tailwind.config.js` 加 `darkMode:'class'`；`<html>` 随 `uiStore.theme` 切换 `dark` 类；Layout 提供切换按钮并持久化到 `config.ui.theme`；主要容器补 `dark:` 变体。
- ✔ **拖拽上传**：MainPage 接收 Tauri `onDragDropEvent` 拖入的 PDF 路径；`tauri.conf.json` 显式 `dragDropEnabled: true`。
- ✔ **导出双语**：新增 `ExportBar` + `utils/export.ts`，支持 Markdown/纯文本导出；Rust `export_content` 命令写盘，浏览器回退 blob 下载。
- ✔ **R14 字段统一**：OCR/翻译配置沿用 snake_case（provider/api_key/api_url/model/...），前后端一致。

**Phase 3 — 工程化与开源**
- R11 测试+CI；R12 清理脚本；`LICENSE`；`.env.example`；`README` 复刻指南（见 Q2）。

---

## 9. 关键决策（已确认，2026-09-04）

> 以下为与用户确认后的**锁定决策**，后续开发必须遵守，agent 不得自行变更。

- **D1 后端分发**：FastAPI 以 **Tauri sidecar** 形式内嵌（Python 解释器 + venv 随 exe 打包），应用启动由 Rust 拉起本地 8000 端口，做到“双击即用、开箱即用”。（对应 R3）
- **D2 目标平台**：分发包**仅 Windows**；文档写清依赖与步骤便于他人参考复刻，但不保证开箱跨平台。
- **D3 翻译 Provider**：采用**可扩展 provider 抽象**（见 4.3）。默认 SiliconFlow；UI 列表提供主流提供商（含 SiliconFlow、OpenAI 及兼容端、Google、DeepL 等）。OpenAI 兼容协议共用一套 chat/completions 实现；Google/DeepL 为独立适配器，未实现前 UI 标注“即将支持”，禁止静默失败。
- **D4 多页呈现**：采用**分页 + 缩略图导航**（上一页/下一页 + 页面缩略图跳转），取代当前仅渲染 `pages[0]` 的缺陷。（对应 R2）
- **D5 文档定位**：本文件（`docs/开发总纲.md`）为唯一事实来源，旧 `pdf-bilingual-reader-dev-guide.md` 已作废。

---

## 10. 构建与验证（重要）

### 10.1 内嵌后端（sidecar）构建流程
打包自包含 exe 前，**必须先**构建后端 sidecar：

```powershell
# 1) 构建内嵌后端 → src-tauri/sidecar/pdf-backend-<triple>.exe
powershell -ExecutionPolicy Bypass -File scripts/build-backend.ps1

# 2) 再打包桌面应用（sidecar 会一并打入）
npm run tauri build
```

`scripts/build-backend.ps1` 会自动探测 `.venv`/`python`、按需安装 PyInstaller、
从 `rustc -vV` 推断目标三元组，并对 uvicorn 做 `--collect-all` 收集。

### 10.2 开发态运行
开发态**不会**拉起内嵌 sidecar（避免与手动启动的后端争用 8000 端口）：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/dev-start.ps1
```

### 10.3 环境要求与已知限制 ⚠️
- **Windows 10/11 SDK 必需**：Rust 的 MSVC 目标链接需要 Windows 10/11 SDK。当前开发机**仅安装了 Windows Kits 8.1**，缺少 10/11 SDK，因此 `cargo check` / `tauri build` **无法在该机完成**（失败在链接阶段，非代码问题）。
- 另注：在 Git Bash 中 `link.exe` 会优先解析到 `/usr/bin/link`（GNU 硬链接命令）而非 MSVC 链接器，应使用 **“VS Developer PowerShell” / “x64 Native Tools”** 环境构建。
- 结论：**本次 Rust 侧改动尚未经编译验证**。请安装 “C++ 生成工具 + Windows 10/11 SDK” 后先执行 `cargo check` 复核，再 `tauri build`。
- 前端已验证通过：`npx tsc -b`（类型检查）与 `npx vite build`（构建）均 exit 0。

---

## 11. OCR 端点验证发现（R6，待选型）⚠️

> 2026-09-04 技术验证结果：**原 OCR 实现调不通，属 P0 级阻断。**

### 11.1 现有实现的错误
`backend/ocr/siliconflow.py` 当前：
```python
payload = {"model": model, "file": file_data, "fileType": 0, "optionalPayload": ...}
resp = client.post(f"{api_url}/generate", ...)
data["result"][...]["layoutParsingResults"]
```
这是 **PaddleOCR 自托管 `layout-parsing` 服务**的请求/响应格式（需本地起服务，端口 8080），**SiliconFlow 云 API 根本没有 `/generate` 端点**。

### 11.2 SiliconFlow 平台的真实调用方式（已查证官方文档）
PaddleOCR-VL-1.5 在 SiliconFlow 上通过标准 OpenAI 兼容接口提供：
```
POST https://api.siliconflow.cn/v1/chat/completions
{
  "model": "PaddlePaddle/PaddleOCR-VL-1.5",
  "messages": [{"role":"user","content":[
     {"type":"image_url","image_url":{"url":"data:image/jpeg;base64,....","detail":"high"}},
     {"type":"text","text":"OCR:"}
  ]}],
  "max_tokens": 8192, "temperature": 0.01
}
→ choices[0].message.content  # markdown 文本
```
**关键约束**：该接口只接受**图片**（image_url），**不接受 PDF 文件**。因此无论怎么改，都必须先把 PDF 每页渲染成图片再送入模型。

### 11.3 由此带来的架构影响
1. 需要在流水线中引入 **PDF→图片** 步骤（建议 `PyMuPDF`/`fitz`，单 pip 包跨平台，无外部依赖）；或用 `pdf2image`（依赖 poppler，不推荐）。
2. OCR 返回的是**整页 markdown 文本流**，不再有 `layoutParsingResults` 的 bbox/分块结构 → 现有 `PageResult.blocks` 数据模型需调整（整页一块 vs 细分块），对照阅读的对齐方式也受影响。
3. 失去精确 bbox 后，若要“译文贴回原位置”需另寻方案（超出当前范围）。

### 11.4 待用户选型的三个方向（见尾声选择题）
- **A（纯 API，推荐起步）**：PyMuPDF 逐页转图 → 逐页调 `/chat/completions` → 整页 markdown 作为一“块”。无需本地模型，最易复刻；代价是无细分块结构、长文档 token 成本高。
- **B（本地 layout 服务）**：保留 `layoutParsingResults` 的多块结构，但需在本地部署 PaddleOCR layout 服务（需 GPU/较大依赖），违背“轻松复刻”。
- **C（混合）**：PyMuPDF 优先提取**文本层**（有文本层的 PDF 零成本、零网络）；无文本层（扫描件）才走 OCR。兼顾速度与成本。

---

> **本文件为项目最高约束。任何开发决策不得违反本文的核心技术选型、数据模型与接口契约。如需修改，必须先更新本文对应章节。**
>
> 最后更新：2026-09-04（Phase 1 推进：R5/R7 完成、缩略图引入 pdfjs-dist、R6 OCR 端点重大发现写入第 11 章）。
