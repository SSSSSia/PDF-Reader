# 重构路线图（v0.4.0 起 · 性能与质量重构）

> **地位**：当前活跃开发计划，位于 `开发总纲.md` 之下、各阶段文档之上。总纲锁定范围与选型不变；本路线图只做工程级重构。
> **依据**：`瓶颈分析报告.md`（2026-09-06 全链路代码审计）。
> **制定日期**：2026-09-06

---

## 1. 重构目标（可量化）

| 维度 | 现状 | 目标 |
|------|------|------|
| 首次可见时间 | 全文翻译完才有任何内容（2–4 分钟） | 文本层 PDF 上传后 **≤ 3 秒**可见全文原文 |
| 译文呈现 | 一次性 | **逐段渐进流入**，边译边读 |
| 术语一致性 | 同一术语跨批次漂移 | 抽检 5 个术语全文译法一致 |
| 双栏对齐 | 字符数估算，错位漂移 | 左右**结构化同行对齐**，零漂移 |
| 稳定性 | _jobs 内存泄漏、截断静默降级 | 连续 10 份 PDF 内存平稳、截断自动重试 |

## 2. 总原则

1. **渐进式重构，不推倒重来**（决策 ADR-R0）：现有栈（Tauri/React/FastAPI/文本层优先/合并批量）经审计 80% 正确，推倒重写是负收益。
2. **每阶段独立可交付**：阶段结束 = 可用版本 + tag + 回填文档，任何时刻停下项目都处于可用状态。
3. **感知优先**：先解决"用户看得到的"（渐进呈现），再解决"用户感受到的"（质量），最后解决"天花板"（渲染路线）。
4. **遵守总纲约束**：不改锁定选型；阶段 4 若要走原生渲染，属新决策 **D6**，必须由用户确认后方可动工。

## 3. 阶段总览

| 阶段 | 主题 | 核心改动 | 预计工作量 | 版本 | 文档 |
|------|------|----------|-----------|------|------|
| 1 | 渐进呈现与性能 | 前端渐进渲染、去遮罩、连接池复用、job 生命周期 | 0.5–1 天 | v0.4.0 | 阶段1-渐进呈现与性能.md |
| 2 | 翻译质量工程 | finish_reason 检查、缓存版本化、跨页段落合并、术语表两遍法 | 1–2 天 | v0.5.0 | 阶段2-翻译质量工程.md |
| 3 | 排版与公式 | 双栏结构化对齐、KaTeX、紧跟模式打磨 | 1–2 天 | v0.6.0 | 阶段3-排版与公式.md |
| 4 | 原生渲染路线评估 | 三方案对比评估 + 原型验证 + D6 决策（**决策门，非直接开发**）✅ 已完成：D6=C 可选模式立项 | 评估 1 天 | v0.7.0 | 阶段4-原生渲染路线评估.md |
| 5 | 原版对照渲染 | D6 立项实施：后端块级 bbox 标注 + 前端 pdfjs 原版模式（可选，与现有模式并存） | 1–2 天 | v0.7.0 | 阶段5-原版对照渲染.md |
| 6 | 文档库与持久化统一 | 配置单源一次填写、持久化文档索引、首页文章列表、缓存/数据目录统一到用户目录；验收期扩展（2026-09-09）：靠岸学术界面改版（侧边栏/文件夹管理/缩略图卡片）、删除二次确认、崩溃可诊断性 | 1–2 天 | v0.8.0 | 阶段6-文档库与持久化统一.md |
| 7 | 阅读体验增强 | 全模式 Ctrl+滚轮缩放、原版默认 70%、模式选择器分组重排版/原版、原版对照页（对齐翻译） | 2–3 天 | v0.9.0 | 阶段7-阅读体验增强.md |
| 8 | 多会话阅读与后台翻译 | 会话注册表（页签多开互不干扰）、翻译后台化（轮询脱离阅读组件）、文献库解除翻译期互斥、会话 LRU 上限 | 1–2 天 | v0.10.0 | 阶段8-多会话阅读与后台翻译.md |
| 9 | BabelDOC 双语 PDF | 后端子进程集成 BabelDOC CLI（SiliconFlow/Qwen3-8B），整篇导出排版一致的双语对照 PDF（图表/公式原位），进度/缓存/取消；AGPL 合规声明 | 1–2 天（T0 前置验证为决策门） | v0.11.0 | 阶段9-BabelDOC双语PDF.md |
| 10 | 桌面沉浸化与自定义标题栏 | decorations:false + 自绘标题栏（拖拽/窗口三钮/文档名）+ 内容区通顶铺满，Scholaread 式桌面观感 | 1 天（T1 mockup 为前置门） | v0.12.0 | 阶段10-桌面沉浸化与自定义标题栏.md |

依赖关系：`1 → 2 → 3 → 4 → 5`（严格串行；阶段 2/3 内部任务可并行，见各阶段文档）。
`5 → 6 → 7`（阶段 6/7 为 2026-09-08 验收期后新增的功能扩展阶段；阶段 7 依赖阶段 6 的文档库与持久化基建）。
`7 → 9 / 10`（阶段 9/10 为 2026-09-10 用户新增：整篇双语导出与桌面沉浸化；二者无相互依赖，可并行；阶段 7 剩余 T5（重排版双栏缩放复验）由阶段 10 T5 吸收——布局基线在阶段 10 改变，合并复验）。

## 4. 状态跟踪表（唯一状态源）

| 阶段 | 状态 | 开始日期 | 完成日期 | 对应 tag |
|------|------|----------|----------|----------|
| 阶段 1 渐进呈现与性能 | 🔶 待人工验收 | 2026-09-06 | — | v0.4.0（验收通过后打） |
| 阶段 2 翻译质量工程 | 🔶 开发完成待人工验收 | 2026-09-07 | — | v0.5.0（验收通过后打） |
| 阶段 3 排版与公式 | 🔶 开发完成待人工验收 | 2026-09-07 | — | v0.6.0（验收通过后打） |
| 阶段 4 原生渲染路线评估 | ✅ 已完成（D6=C 可选模式立项） | 2026-09-07 | 2026-09-07 | —（决策产出 D6） |
| 阶段 5 原版对照渲染 | 🔶 开发完成待人工验收 | 2026-09-07 | — | v0.7.0（验收通过后打） |
| 阶段 6 文档库与持久化统一 | 🔶 开发完成待人工验收 | 2026-09-08 | — | v0.8.0（验收通过后打） |
| 阶段 7 阅读体验增强 | 🚧 进行中（T1–T4 完成，T5 并入阶段10复验） | 2026-09-09 | — | v0.9.0 |
| 阶段 8 多会话阅读与后台翻译 | 🔶 待人工验收 | 2026-09-09 | — | v0.10.0（验收通过后打） |
| 阶段 9 BabelDOC 双语 PDF | 🚧 进行中（T0 决策门 + T1–T3 完成，T4/T5 待做） | 2026-09-10 | — | v0.11.0 |
| 阶段 10 桌面沉浸化与自定义标题栏 | 🔶 开发完成待人工验收 | 2026-09-09 | — | v0.12.0（验收通过后打） |

图例：⬜ 未开始 ｜ 🚧 进行中 ｜ 🔶 待人工验收 ｜ ✅ 已完成（含文档回填 + tag）

### 4.1 验收期修复记录（不新开阶段，归入对应阶段文档）

2026-09-08 人工验收期间发现并修复的问题集中记录于 `阶段5-原版对照渲染.md` §5.2：
公式按需 OCR 完整链路与失败根修、提取层伪标题降级/斜体误判上标还原/跨栏粘连段拆分
（TEXT_LAYER_MODEL v15→v16）、批次翻译 `<<<n>>>` 分隔标记协议（PROMPT_VERSION pv4→pv5）。
沉淀两个可重复工具：`backend/tools/extraction_health.py`（提取回归护栏）、
`backend/tools/ab_textlayer_vs_ocr.py`（选型对比）。下一阶段重心见 `版本规划.md` §2.1 候选池。

2026-09-09 阶段 6 验收期界面改版与稳定性（归入 `阶段6-文档库与持久化统一.md` T5–T7）：
主页/添加文章页对齐"靠岸学术"参考排版（commit 90c37a3）→ 侧边栏 + 文件夹管理一比一复刻
（264bd6c / b37453e）→ 空态与返回导航修复（2fc4fb3）→ 删除二次确认（自绘 ConfirmDialog，
**Tauri WebView2 的 window.confirm 不可靠，原生对话框一律自绘**）+ 崩溃可诊断性
（ErrorBoundary 详情 + 前端错误上报 `data_dir/logs/frontend.log`，13598a4）。
遗留：用户报告的上传页崩溃根因待复现后凭错误详情/日志定位。
沉淀约束：UI 红线（蓝色仅交互语义、原生对话框自绘、pdfStore 单会话写入互斥）。
~~pdfStore 单会话写入互斥~~：已被阶段 8 多会话架构取代（2026-09-09）——互斥守卫删除，
pdfStore 降级为「唯一活跃会话」载体，多开会话由 sessionsStore 快照交换管理。

2026-09-09 阶段 8 多会话阅读与后台翻译开发完成（T1–T4 全过，T5 自动化回归通过）：
sessionsStore（会话注册表 + 快照交换，MAX_SESSIONS=8 LRU）、translationManager（轮询
后台化，同时仅 1 个翻译任务）、ReaderTabs（页签条，翻译中徽标不可关闭）、MainPage/
AddArticlePage 接入（解除翻译期互斥、进度卡 runningJob 驱动）、useOcr.ts 退役。
自动化验收：tsc 0 错误 / vite build 通过 / pytest 143 全过；人工验收清单见阶段 8 文档 §4。

## 5. 阶段门禁（每阶段必须全部满足才算完成）

1. 阶段文档内所有任务 checkbox 勾选（或明确标注放弃 + 原因）。
2. 验收标准逐条通过（阶段文档定义的可量化指标）。
3. 人工测试清单填写完毕（模板见 `开发流程管理.md`）。
4. `npx tsc --noEmit` + `cargo check`（有 Rust 改动时）通过。
5. **完成记录回填**到阶段文档（日期、文件清单、遗留问题）。
6. 合并 `dev → master`，打 tag（按 `版本规划.md` 语义化版本）。

## 6. 风险清单

| 风险 | 影响 | 缓解 |
|------|------|------|
| 轮询全量 pages 的载荷随文档增大 | 网络与渲染压力 | 阶段 1 先按 1s 轮询上线；若大文档卡顿，阶段 2 提供 SSE 替代 |
| 术语抽取多一次 LLM 请求 | 成本与首译时延 +1 请求 | 术语表按 pdf_hash 缓存，仅首次；失败时降级为无术语表直译 |
| 跨页段落合并误判 | 段落错误拼接 | 保守规则（仅句尾无终止符且下页非结构块时合并），合并处留日志 |
| KaTeX 对 pymupdf4llm 输出无效（公式非 LaTeX 源） | 公式改善有限 | 阶段 3 只承诺 `$...$` 语法可渲染；根治在阶段 4 决策 |
| 阶段 4 立项后工程量超预期 | 排期失控 | 决策门设计：先 1–2 天原型 spike 拿数据，再定立项 |

## 7. 与既有文档的关系

- `开发总纲.md`：范围、选型、接口契约不变；总纲第 8 章路线图（Phase 0–3）已基本完成，本路线图是**其后继计划**。
- `版本规划.md`：v0.4.0–v0.7.0 版本号已登记（见该文档第 2 节更新）。
- `瓶颈分析报告.md`：本路线图的立项依据，不再更新（快照性质）。
