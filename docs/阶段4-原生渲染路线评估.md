# 阶段 4：原生渲染路线评估（决策门 · D6）

> **状态**：⬜ 未开始 ｜ **前置**：阶段 1–3 全部完成且用户人工验收满意度的**天花板仍不满足**时才启动
> **性质**：**这是决策文档，不是开发任务**。产出是 D6 决策记录，不是代码。
> **硬约束**：总纲第 2 章锁定技术栈、第 7.1 条"不得替换已锁定技术栈"——本阶段任何结论需以**新决策 D6** 形式经用户确认后方可动工。

---

## 1. 背景与证据

- Markdown 重建路线（pymupdf4llm → 自排版）的天花板：`position` 丢坐标、多栏阅读顺序偶发串行、矢量图形无法提取、PDF 原生公式是 Unicode 碎字符。
- 知云/靠岸的 100% 保真来自：PDF 原生渲染 + 文本层坐标提取 + 译文按坐标对照。
- 阶段 1–3 完成后本路线预期可达 ~80 分（干净、灵活、成本可控）；是否冲最后 20 分，取决于本评估。

## 2. 候选方案

| 方案 | 描述 | 保真度 | 工程量 | 风险 |
|------|------|--------|--------|------|
| **A 维持现状** | 阶段 1–3 后的 Markdown 路线，持续小修 | ~80 分 | 0 | 无；接受天花板 |
| **B 混合坐标路线** | PyMuPDF `page.get_text("dict")` 提取 block bbox → 页面渲染为背景图 → 译文按 bbox 贴坐标 overlay（双栏模式用，紧跟模式保留 Markdown 流） | ~90 分 | 中（1–2 周） | bbox 合并策略复杂（一个视觉段落多个 bbox）；多栏顺序仍需推断 |
| **C PDF.js 原生渲染** | pdfjs-dist 渲染页面（已引入，现仅缩略图）+ 文本层 span 定位 + 译文行内替换/对照（知云模式） | ~100 分 | 高（2–4 周） | 文本选择/字体嵌入/双栏重排交互复杂；与 Tauri webview 兼容需验证 |

## 3. 评估维度（决策时逐项打分）

1. **保真度**：多栏、公式、矢量图、表格、页眉页脚的还原度（用 3 份典型论文实测）。
2. **工作量**：含学习曲线与调试的日历时间。
3. **风险**：与现有 Tauri/React 架构的兼容性、性能（大 PDF 渲染内存）。
4. **可回退性**：B/C 是否可以只作为"双栏模式"的渲染层、紧跟模式保留现有实现（建议：是，天然降级路径）。
5. **维护成本**：开源复刻者能否按文档复现。

## 4. 原型验证计划（Spike，1–2 天，决策前必做）

- [x] S1：用 PyMuPDF 提取 3 份双栏论文（DALK、HippoRAG、TOG）的 block bbox + 文本，验证与 pymupdf4llm 输出的段落一一对应（B 方案可行性核心）。脚本已归档：`scripts/spike_s1_bbox_check.py`。
- [x] S2：pdfjs-dist 6.3.289 全页渲染 + 文本层 span 定位 demo（`docs/spike-s2/`，样张 DALK.pdf 第 7 页图表密集页）。已在 Chromium（agent-browser CDP）跑通；**Tauri WebView2 实测待用户亲自验证**（demo 页内含提示）。
- [x] S3：实测数据回填本表：

| 验证项 | 结果 |
|--------|------|
| bbox 与段落对应率 | **DALK 75%（块）/ 73%（段）；HippoRAG 77% / 72%；TOG 32% / 66%**——全部低于预注册阈值 90%，未匹配项主要是公式碎块与图内文本框（后者本应由快照机制兜住，TOG 图内文字占比高所以段级劣化明显）→ **B 方案不达标** |
| 双栏阅读顺序正确率 | 本次未单独测。对 B/C 的"贴原版布局"形态，阅读顺序由用户视觉决定，不依赖提取顺序；现有管线（阶段 1–3 合并扫描）已具备双栏顺序能力可复用 |
| Tauri webview 渲染性能 | Chromium（CDP headless，dpr=1）实测：**冷启动首页 0.6–2.0 s（worker/字体初始化），热翻页渲染 38 ms + 文本层 33 ms**；文本层 193 项 → 174 个 span 构建仅 59–75 ms；堆内存 2.6–2.9 MB。pdfjs 6 span 用 `left/top` 百分比 + `--font-height`/`--scale-x` CSS 变量定位，文本层与画布 1px 内对齐；点击 span 可精确取坐标。WebView2 同为 Chromium 内核，预期一致，待实机确认 |
| Spike 耗时 | S1 批量核对 3 份论文 ≈ 43 s；S2 渲染单页 0.6–2.0 s（冷）/ 38 ms（热） |

### 4.1 Spike 附加发现

1. **渲染保真度（S2 截图为证）**：表格、双栏正文、图表（柱状/折线/坐标轴/图例）全部原生像素级还原——这是提取式路线（A）永远达不到的，公式显示问题在 C 方案下天然消失。
2. **pdfjs 6 的坑**：`getDocument({ cMapUrl: "NONE" })` 会直接抛 `Invalid factory url`（URL 必须带结尾斜杠），传 "NONE" 占位会中断加载——可选参数应整体省略。
3. **内存与性能不构成风险**：单页堆内存 < 3 MB，热翻页 < 100 ms，远低于可感知阈值；C 方案"webview 性能"kill criteria 不触发。
4. **S1 的 90% 线判死 B 方案**：bbox 合并策略再怎么优化，公式碎块与图内文本的归属混乱是提取层的固有缺陷，与合并算法无关。

## 5. 决策规则（预注册，避免沉没成本绑架）

- Spike 中 **B 方案 bbox 对应率 ≥ 90%** 且用户认为 90 分够用 → 选 B。
- 用户明确要知云级体验（100 分）且接受 2–4 周 → 选 C。
- 两个 Spike 都不及预期 或 用户对阶段 1–3 的 80 分已满意 → 选 A（本阶段关闭，路线图终版）。
- **Kill criteria**：任何方案实施中途发现核心假设不成立（如 bbox 合并不可行 / webview 性能不可接受），立即停下回到 A，不追加沉没成本。

## 6. 交付物

1. 本文档的 Spike 数据回填 + 方案对比结论。
2. **D6 决策记录**（追加到 `开发总纲.md` 第 9 章决策列表，格式对齐 D1–D5），含用户确认日期。
3. 若立项（B/C）：新建对应阶段文档（复制 `阶段文档模板.md`），进入正常阶段流程；预计版本 v0.7.0。
4. 若选 A：本路线图整体收官，输出终版总结。

## 7. 完成记录（决策完成后填写）

- 评估日期：2026-09-07
- Spike 数据结论：S1 bbox↔段落对应率 DALK 75%/73%、HippoRAG 77%/72%、TOG 32%/66%（块/段）——全部低于 90% 预注册线，**B 方案不达标**；S2 pdfjs 6.3.289 全页渲染可行：热翻页 38 ms、文本层 33 ms、堆内存 <3 MB、span 坐标 1 px 对齐（详见 §4/§4.1）。
- 选定方案：**C 方案（pdfjs 原生渲染），以可选「原版对照」模式立项，与现有提取式模式并存**——用户裁决"做成可选的方案，现有的翻译效果也保留"，对应预注册规则第 2 条（知云级体验 + 接受工程量），且天然满足第 4 条降级路径。
- D6 确认人 / 日期：用户，2026-09-07（已同步 `开发总纲.md` 第 9 章决策列表，pdfjs-dist 用途约束 7.10 同步修订）。
- 后续阶段文档链接：`阶段5-原版对照渲染.md`（目标版本 v0.7.0）。
